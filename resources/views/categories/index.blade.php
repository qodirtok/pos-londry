@extends('layouts.app')
@section('title','Kategori')
@section('content')
<div class="flex justify-between items-center mb-4"><h1 class="text-xl font-bold">Kategori</h1><a href="{{ route('categories.create') }}" class="bg-indigo-600 text-white px-4 py-2 rounded-xl text-sm">+ Kategori</a></div>
<div class="bg-white rounded-2xl border overflow-hidden"><table class="w-full text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="text-left p-3">Kode</th><th class="text-left">Nama</th><th>Status</th><th></th></tr></thead><tbody>@foreach($categories as $c)<tr class="border-t"><td class="p-3 font-mono text-xs">{{ $c->code }}</td><td class="font-medium">{{ $c->name }}</td><td><span class="px-2 py-1 rounded-full text-xs bg-slate-100">{{ $c->status }}</span></td><td class="text-right p-3 flex justify-end items-center gap-2"><a href="{{ route('categories.edit',$c) }}" class="text-indigo-600 text-xs">Edit</a><form method="POST" action="{{ route('categories.destroy',$c) }}" onsubmit="return confirm('Hapus kategori {{ $c->name }}?')">@csrf @method('DELETE')<button class="text-rose-600 text-xs">Hapus</button></form></td></tr>@endforeach</tbody></table><div class="p-3">{{ $categories->links() }}</div></div>
@endsection
