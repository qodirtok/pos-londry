@extends('layouts.app')
@section('title','Users')
@section('content')
<div class="flex justify-between items-center mb-4"><h1 class="text-xl font-bold">Users</h1><a href="{{ route('users.create') }}" class="bg-indigo-600 text-white px-4 py-2 rounded-xl text-sm">+ User</a></div>
<div class="bg-white rounded-2xl border overflow-hidden"><table class="w-full text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="text-left p-3">Nama</th><th>Username</th><th>Email</th><th>Role</th><th>Cabang</th><th>Status</th><th></th></tr></thead><tbody>@foreach($users as $u)<tr class="border-t"><td class="p-3 font-medium">{{ $u->name }}</td><td class="font-mono text-xs">{{ $u->username }}</td><td class="text-xs">{{ $u->email }}</td><td class="text-xs">{{ $u->roles->pluck('name')->join(', ') }}</td><td class="text-xs">{{ $u->branch->name ?? '-' }}</td><td><span class="px-2 py-1 rounded-full text-xs {{ $u->status=='active'?'bg-emerald-100 text-emerald-700':'bg-rose-100' }}">{{ $u->status }}</span></td><td class="text-right p-3"><a href="{{ route('users.edit',$u) }}" class="text-indigo-600 text-xs">Edit</a></td></tr>@endforeach</tbody></table><div class="p-3">{{ $users->links() }}</div></div>
@endsection
