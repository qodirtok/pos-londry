@extends('layouts.app')
@section('title','Cabang')
@section('content')
<div class="flex justify-between items-center mb-4"><h1 class="text-xl font-bold">Cabang</h1><a href="{{ route('branches.create') }}" class="bg-indigo-600 text-white px-4 py-2 rounded-xl text-sm">+ Cabang</a></div>
<div class="bg-white rounded-2xl border overflow-hidden"><table class="w-full text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="text-left p-3">Kode</th><th class="text-left">Nama</th><th>Kota</th><th>Status</th><th></th></tr></thead><tbody>@foreach($branches as $b)<tr class="border-t"><td class="p-3 font-mono">{{ $b->code }}</td><td class="font-medium">{{ $b->name }}</td><td class="text-center">{{ $b->city }}</td><td class="text-center"><span class="px-2 py-1 rounded-full text-xs {{ $b->status=='active'?'bg-emerald-100 text-emerald-700':'bg-slate-100' }}">{{ $b->status }}</span></td><td class="text-right p-3 flex justify-end items-center gap-2"><a href="{{ route('branches.edit',$b) }}" class="text-indigo-600 text-xs">Edit</a><form method="POST" action="{{ route('branches.destroy',$b) }}" onsubmit="return confirm('Hapus cabang {{ $b->name }}?')">@csrf @method('DELETE')<button class="text-rose-600 text-xs">Hapus</button></form></td></tr>@endforeach</tbody></table><div class="p-3">{{ $branches->links() }}</div></div>
@endsection
