@extends('layouts.app')
@section('title','Kas')
@section('content')
<div class="flex justify-between items-center mb-4"><h1 class="text-xl font-bold">Kas Masuk / Keluar</h1><a href="{{ route('cash.create') }}" class="bg-indigo-600 text-white px-4 py-2 rounded-xl text-sm">+ Transaksi</a></div>
<form class="bg-white rounded-2xl border p-4 mb-4 flex flex-wrap gap-2"><select name="type" class="border rounded-xl px-3 py-2"><option value="">Semua</option><option value="income" @selected(request('type')=='income')>income</option><option value="expense" @selected(request('type')=='expense')>expense</option></select><input name="from" type="date" value="{{ request('from') }}" class="border rounded-xl px-3 py-2"><input name="to" type="date" value="{{ request('to') }}" class="border rounded-xl px-3 py-2"><button class="bg-slate-900 text-white px-4 py-2 rounded-xl">Filter</button></form>
<div class="bg-white rounded-2xl border overflow-hidden"><table class="w-full text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="text-left p-3">Tanggal</th><th class="text-left">Kategori</th><th>Tipe</th><th class="text-right">Nominal</th><th>Keterangan</th></tr></thead><tbody>@forelse($transactions as $t)<tr class="border-t"><td class="p-3">{{ $t->transaction_date->format('d/m/Y') }}</td><td>{{ $t->category ?? '-' }}</td><td><span class="px-2 py-1 rounded-full text-xs {{ $t->type=='income'?'bg-emerald-100 text-emerald-700':'bg-rose-100 text-rose-700' }}">{{ $t->type }}</span></td><td class="text-right font-medium">{{ money($t->amount) }}</td><td class="text-xs">{{ $t->description }}</td></tr>@empty<tr><td colspan="5" class="p-6 text-center text-slate-400">Belum ada transaksi</td></tr>@endforelse</tbody></table><div class="p-3">{{ $transactions->withQueryString()->links() }}</div></div>
@endsection
